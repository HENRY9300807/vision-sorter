# GitHub 실시간 동기화 사용법 (최종)

로컬 `analysis_color` 폴더와 GitHub 레포지토리(`HENRY9300807/vision-sorter`)를 실시간으로 동기화합니다.

## 핵심 원칙

> **Cursor/로컬에서 작업하는 폴더와, PowerShell에서 실행하는 폴더와, GitHub에서 동기화되는 리포가 반드시 동일한 워킹카피여야 한다.**

가장 안전하고 되돌리기 쉬운 방식은 **현재 `analysis_color` 폴더를 그대로 Git 리포로 만들고, 원격을 `vision-sorter`에 연결한 뒤(새 브랜치), 자동 푸시를 얹는 것**입니다.

(기존 원격의 `main`을 덮지 않고 **새 브랜치**로 올리므로 사고를 방지)

---

## ✅ 0) 준비: PowerShell 열기 + 폴더 이동

```powershell
# PowerShell (관리자 아님) 실행
cd "$env:USERPROFILE\Desktop\analysis_color"
```

> 만약 여기서 `git`이 안 잡히면 Git for Windows 설치 필요  
> 만약 Python이 필요한 다른 작업이 있으면 `py -V`로 확인 (동기화 자체엔 필요 없음)

---

## ✅ 1) 안전백업 (30초)

```powershell
if (-not (Test-Path .\.git)) {
  git init
}
git add -A
git commit -m "backup: local snapshot before linking to vision-sorter" --allow-empty
git tag backup-pre-link
```

---

## ✅ 2) 원격(origin) 연결 → 현재 브랜치로 초기 푸시

> **참고:** 현재 기본 브랜치는 `henry/analysis_color`입니다. 이 브랜치를 사용하거나 필요시 `main`으로 변경할 수 있습니다.

```powershell
# 원격 설정 (이미 있다면 set-url)
if ((git remote) -notcontains 'origin') {
  git remote add origin https://github.com/HENRY9300807/vision-sorter.git
} else {
  git remote set-url origin https://github.com/HENRY9300807/vision-sorter.git
}

git fetch origin

# 현재 브랜치 확인
git branch --show-current

# 최초 업로드 (현재 브랜치로)
git add -A
git commit -m "chore: import local analysis_color working copy"
git push -u origin $(git branch --show-current)
```

> **브랜치 변경:** `main` 브랜치를 사용하려면 `git checkout -b main && git push -u origin main` 후 GitHub 설정에서 기본 브랜치를 `main`으로 변경하세요.

---

## ✅ 3) (권장) .gitignore 확인

작업 폴더에 **.gitignore**가 이미 추가되어 있어 불필요한 파일이 올라가지 않습니다.

확인:
```powershell
cat .gitignore
```

필요시 수정:
```powershell
notepad .gitignore
```

---

## ✅ 4) "파일 수정 → 자동 동기화" 2가지 중 택1

### A) **Cursor Git 패널** 사용(추천)

* **File 저장(Ctrl+S)** → 좌하단 *Git Changes*
* **Commit 메시지 입력 → Push** 클릭
* 끝. (가장 안정적, 커밋 단위 관리가 깔끔)

> 네 Cursor 버전에 **자동 stage/commit/push** 옵션이 있다면 켜도 되지만,  
> 커밋 메시지를 통제하기 어려워서 기본은 수동 Commit & Push를 권장.

### B) **PowerShell 자동 푸시 워처(원터치)**

파일 변경을 감지해 **바뀐 것만** 커밋/푸시한다(디바운스 포함).

**실행:**

```powershell
pwsh .\auto-sync.ps1
```

> 언제든 창 닫거나 `Ctrl+C`로 중단.  
> 브랜치가 `henry/analysis_color`로 트래킹되어 있으니 그 브랜치로 계속 밀린다.

**옵션 (디바운스 시간 조정):**

```powershell
# 기본 1.5초 대기
pwsh .\auto-sync.ps1

# 3초 대기 (더 안정적)
pwsh .\auto-sync.ps1 -DebounceMs 3000
```

---

## ✅ 5) 이후 **PowerShell 실행 테스트**도 같은 폴더에서

항상 이 폴더에서:

```powershell
cd "$env:USERPROFILE\Desktop\analysis_color"
python main.py   # 또는 py main.py
```

> 이렇게 하면 **Cursor에서 수정한 코드가 100% 동일한 위치**에서 실행된다.

---

## 🔎 혹시 이미 `vision-sorter`를 따로 클론해둔 경우?

두 방법 중 하나만 사용:

* **방법 1(위 절차 추천)**: `analysis_color`를 원격에 연결해 브랜치로 올림 → 필요 시 PR로 합치기
* **방법 2(대안)**: `vision-sorter` 워킹카피로 작업 폴더를 바꾸고(= Cursor/PowerShell 모두 그 폴더 사용), `analysis_color` 내용물을 그 안으로 이동 후 커밋/푸시

---

## GitHub 인증 설정

### Personal Access Token 사용

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 새 토큰 생성 (권한: `repo`)
3. 토큰을 환경변수로 설정:

```powershell
# Windows PowerShell
$env:GITHUB_TOKEN = "your_token_here"
```

또는 Git Credential Manager 사용:

```powershell
git config --global credential.helper manager-core
```

### SSH 키 사용

```powershell
# SSH 키 생성 (아직 없다면)
ssh-keygen -t ed25519 -C "your_email@example.com"

# GitHub에 SSH 키 추가
# GitHub → Settings → SSH and GPG keys → New SSH key
```

---

## 문제 해결

### "Git 저장소가 아닙니다" 오류

```powershell
cd "$env:USERPROFILE\Desktop\analysis_color"
git init
```

### "원격 저장소가 설정되지 않았습니다" 경고

```powershell
git remote add origin https://github.com/HENRY9300807/vision-sorter.git
```

### 푸시 실패

- GitHub 인증 확인
- 네트워크 연결 확인
- 브랜치 이름 확인 (`henry/analysis_color`)

### Pull 실패 (충돌)

```powershell
# 수동으로 충돌 해결
git status
git pull origin henry/analysis_color
# 충돌 파일 수정 후
git add .
git commit -m "Resolve conflicts"
```

---

## ✅ 지금 바로 실행 체크리스트

* [ ] PowerShell에서 `analysis_color`로 이동
* [ ] 백업 커밋 + 태그 생성
* [ ] 원격을 `vision-sorter`로 연결, 새 브랜치 생성
* [ ] 최초 푸시 성공
* [ ] Cursor에서 같은 폴더 열기 → 수정 → Commit & Push 또는 `auto-sync.ps1`
* [ ] GitHub에서 브랜치 확인 / 필요 시 PR

이 플로우로 가면 **저장 → GitHub 동기화**가 깔끔하게 완성된다.

중간에 충돌/에러가 보이면 **에러 메시지 전체**를 붙여줘—그 상태에서 바로 해결 패치까지 만들어 줄게.
